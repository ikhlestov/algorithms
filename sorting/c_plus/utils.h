#include <fstream>
#include <string>
#include <vector>
#include <stdexcept>
using std::vector;
using std::string;

vector<int> load_data(string data_type);
string join(string joining, vector<string> vec_to_join);

#include <fstream>
#include <string>
#include <vector>
#include <stdexcept>
#include <map>
using std::vector;
using std::string;
using std::map;

vector<int> load_data(string data_type);
string join(string joining, vector<string> vec_to_join);
